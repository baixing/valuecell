{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uvicorn valuecell.server.main:app --host 0.0.0.0 --port $PORT --redirect-slashes false",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
